<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.us.acm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service android:name="org.rowanacm.android.MessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name="org.rowanacm.android.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.rowanacm.android.AttendanceActivity"
            android:label="@string/title_activity_attendance"
            android:parentActivityName="org.rowanacm.android.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.rowanacm.android.MainActivity" />
        </activity>
        <activity
            android:name="org.rowanacm.android.SettingsActivity"
            android:parentActivityName="org.rowanacm.android.MainActivity"
            android:label="@string/title_activity_settings" />
    </application>

</manifest>